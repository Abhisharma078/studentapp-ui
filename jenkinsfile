pipeline {
    agent {
        label "worker"
    }

    stages {
        stage("checkout"){
            steps{
                git 'https://github.com/mayurpandit25/studentapp-ui.git'
                echo "Clonning is successful"
            }
        }
    }
}